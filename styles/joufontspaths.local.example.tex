% Local font-path overrides for non-standard installations.
% Rename this file to styles/joufontspaths.local.tex and adjust the paths.
% The file is git-ignored and intended for machine-specific configuration.

% Example: Windows WPS installed on D:
% \renewcommand{\JOUWPSFontDir}{D:/Apps/WPS Office/office6/fonts}

% Example: Windows system font directory moved or mirrored:
% \renewcommand{\JOUWindowsFontDir}{D:/Windows/Fonts}

% Example: self-managed commercial font pack:
% \renewcommand{\JOUProprietaryFontDir}{D:/Fonts/JOU-Licensed}
